version: 2

before:
  hooks:
    - go mod tidy

builds:
  - id: workflow-plugin-erp
    main: ./cmd/workflow-plugin-erp
    binary: workflow-plugin-erp
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - formats: [tar.gz]
    name_template: "{{ .ProjectName }}-{{ .Os }}-{{ .Arch }}"
    files:
      - plugin.json
      - LICENSE

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
